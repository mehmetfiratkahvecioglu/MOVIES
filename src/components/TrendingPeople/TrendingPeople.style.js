import {StyleSheet} from 'react-native';
import Constants from '../../Constants';
const styles = StyleSheet.create({
  title: {color: Constants.fadedColor, fontSize: 16, margin: 5},
});

export default styles;
